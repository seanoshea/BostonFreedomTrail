machine:
  xcode:
    version: 7.3.1
dependencies:
  pre:
    - gem install bundler -v 1.12.5
    - gem install activesupport -v 5.0.0
  post:
    - bundle exec fastlane enable_crash_reporting
test:
  override:
    - set -o pipefail && bundle exec fastlane test
